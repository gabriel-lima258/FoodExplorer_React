import styled from 'styled-components';

export const Container = styled.span`

    .ingredients {
        padding-inline: 1rem;
    }

    img {
        display: flex;
        margin: auto;
        max-width: 6rem;
        max-height: 6rem;
        margin-bottom: 0.8rem;
    }

    span {
        font-size: 1.8rem;
        font-weight: 400;
        line-height: 140%;
        text-align: center;
    }
`